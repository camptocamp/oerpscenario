<?xml version="1.0" encoding="utf-8"?>
<openerp>
  <data>

    <record id="view_order_form" model="ir.ui.view">
      <field name="name">sale.order.form</field>
      <field name="model">sale.order</field>
      <field name="inherit_id" ref="sale.view_order_form"/>
      <field name="arch" type="xml">
        <field name="client_order_ref" position="after">
          <field name="client_order_contact_type"/>
          <field name="client_order_date"/>
        </field>
        <xpath expr="//div[@name='carrier_selection']" position="after">
          <field name="delivery_term"/>
        </xpath>
<!--         <xpath expr="//div[@name='discount']" position="after">
            <field name="project_discount" groups="sale.group_disount_per_so_line"/>
            <field name="public_discount" groups="sale.group_disount_per_so_line"/>
        </xpath> --> <!-- TOTO Add the discount fields in base view -->
      </field>
    </record>

  </data>
</openerp>
