<?xml version="1.0" encoding="utf-8"?>
<openerp>
  <data>

    <record id="view_order_form" model="ir.ui.view">
      <field name="name">sale.order.form</field>
      <field name="model">sale.order</field>
      <field name="inherit_id" ref="sale.view_order_form"/>
      <field name="arch" type="xml">
        <field name="client_order_ref" position="after">
          <field name="client_order_contact_type"/>
          <field name="client_order_date"/>
        </field>
        <xpath expr="//div[@name='carrier_selection']" position="after">
          <field name="delivery_term"/>
        </xpath>
         <!-- In Form view Remove label "discount"-->
        <xpath expr="//label[@for='discount']" position="replace">
        </xpath>
        <!-- In form view -->
        <xpath expr="//div[@name='discount']" position="replace">
          <field name="discount" class="oe_inline" invisible="1"/>
          <label for="project_discount" groups="sale.group_discount_per_so_line"/>
          <div name="project_discount" groups="sale.group_discount_per_so_line">
            <field name="project_discount" class="oe_inline"/> %%
          </div>

          <label for="public_discount" groups="sale.group_discount_per_so_line"/>
          <div name="public_discount" groups="sale.group_discount_per_so_line">
            <field name="public_discount" class="oe_inline"/> %%
          </div>
        </xpath>
        <!-- Tree view of order_lines used in SO -->
        <xpath expr="//tree/field[@name='discount']" position="replace">
          <field name="discount" groups="sale.group_discount_per_so_line" invisible="1"/>
          <field name="public_discount" groups="sale.group_discount_per_so_line"/>
          <field name="project_discount" groups="sale.group_discount_per_so_line"/>
        </xpath>
      </field>
    </record>

  </data>
</openerp>
