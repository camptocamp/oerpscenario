<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="invoice_form" model="ir.ui.view">
      <field name="name">account.invoice.form</field>
      <field name="model">account.invoice</field>
      <field name="inherit_id" ref="account.invoice_form"/>
      <field name="arch" type="xml">

        <xpath expr="//tree/field[@name='discount']" position="replace">
          <field name="discount" groups="sale.group_discount_per_so_line" invisible="1"/>
          <field name="project_discount" groups="sale.group_discount_per_so_line"/>
          <field name="public_discount" groups="sale.group_discount_per_so_line"/>
        </xpath>

      </field>
    </record>

    <record id="view_invoice_line_form" model="ir.ui.view">
      <field name="name">account.invoice.line.form</field>
      <field name="model">account.invoice.line</field>
      <field name="inherit_id" ref="account.view_invoice_line_form"/>
      <field name="arch" type="xml">

        <xpath expr="//field[@name='discount']" position="after">
          <field name="project_discount" groups="sale.group_discount_per_so_line"/>
          <field name="public_discount" groups="sale.group_discount_per_so_line"/>
        </xpath>

      </field>
    </record>

</odoo>
