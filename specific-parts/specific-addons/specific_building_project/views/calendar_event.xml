<?xml version="1.0" encoding="UTF-8"?>
<odoo>

  <record id="view_crm_meeting_search" model="ir.ui.view">
    <field name="name">CRM - Meetings Search</field>
    <field name="model">calendar.event</field>
    <field name="inherit_id" ref="calendar.view_calendar_event_search"/>
    <field name="arch" type="xml">
      <xpath expr="//field[@name='user_id']" position="after">
        <field name="building_project_id"/>
        <filter name="phonecall" string="Phonecalls" domain="[('categ_ids', 'in', [%(specific_building_project.event_type_phonecall)d])]"/>
      </xpath>
    </field>
  </record>

</odoo>
